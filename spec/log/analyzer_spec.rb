require 'spec_helper'

describe Log::Analyzer do
  it 'has a version number' do
    expect(Log::Analyzer::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
